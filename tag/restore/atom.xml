<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Tag: restore | Edmonds Commerce Dev Blog]]></title>
  <link href="http://edmondscommerce.github.io/tag/restore/atom.xml" rel="self"/>
  <link href="http://edmondscommerce.github.io/"/>
  <updated>2013-12-06T19:10:15+00:00</updated>
  <id>http://edmondscommerce.github.io/</id>
  <author>
    <name><![CDATA[EdmondsCommerce Development Team]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Ubuntu Restore Gnome Panels Script]]></title>
    <link href="http://edmondscommerce.github.io/ubuntu/ubuntu-restore-panels-script.html"/>
    <updated>2010-05-05T09:19:39+01:00</updated>
    <id>http://edmondscommerce.github.io/ubuntu/ubuntu-restore-panels-script</id>
    <content type="html"><![CDATA[<div class="oldpost"><h4>This is an old post. The information it contains is probably out of date or innacurate</h4>
<p>
This is a post that was written a long time ago and is only being kept here for posterity.
You should probably look up more recent blog posts related to the subject you are researching
</p>
</div>


<p>If your ubuntu panels go wonky check out this little script courtesy of <a href="http://bithacker.posterous.com:">http://bithacker.posterous.com:</a></p>

<p>```</p>

<h1>!/bin/sh</h1>

<p>#</p>

<h1>GNOME Panel Save / Restore</h1>

<h1>Writen by PhrankDaChicken</h1>

<p>#</p>

<h1><a href="http://ubuntu.online02.com">http://ubuntu.online02.com</a></h1>

<p>#
#</p>

<h1>Updated to add restore defaults by jimjimovich</h1>

<h1><a href="http://www.starryhope.com">http://www.starryhope.com</a></h1>

<p>#
#</p>

<p>DIR=$(pwd)
TITLE=&ldquo;PanelRestore&rdquo;</p>

<p>Main () {</p>

<pre><code>CHOICE=$(zenity --list --title "$TITLE" --hide-column 1 --text "What do you want to do?" --column "" --column "" \
</code></pre>

<p>&ldquo;0&rdquo; &ldquo;Save Panel Settings&rdquo; \
&ldquo;1&rdquo; &ldquo;Restore Panel Settings&rdquo; \
&ldquo;2&rdquo; &ldquo;Restore Default Panel Settings&rdquo;)</p>

<pre><code>if [ $CHOICE = 0 ]; then
    Panel_Save
fi
if [ $CHOICE = 1 ]; then
    Panel_Restore
fi
if [ $CHOICE = 2 ]; then
    Panel_Defaults
fi  
</code></pre>

<p>}</p>

<p>Panel_Restore () {</p>

<pre><code>FILE=$(zenity --title "$TITLE: Open File" --file-selection --file-filter "*.xml" )
if [ -n "$FILE" ]; then 
    gconftool-2 --load "$FILE"
    killall gnome-panel
fi
Main
</code></pre>

<p>}</p>

<p>Panel_Save () {</p>

<pre><code>FILE=$(zenity --title "$TITLE: Save File" --file-selection --save --confirm-overwrite --filename "Gnome_Panel.xml" --file-filter "*.xml" )
if [ -n "$FILE" ]; then 
    EXT=$(echo "$FILE" | grep "xml")
    if [ "$EXT" = "" ]; then
        FILE="$FILE.xml"
    fi
    gconftool-2 --dump /apps/panel &gt; $FILE
    zenity --info --title "$TITLE: File Saved" --text "File saved as: \n $FILE"
fi
Main
</code></pre>

<p>}</p>

<p>Panel_Defaults () {</p>

<pre><code>zenity --question --text="Are you sure you want to restore the default top and bottom panels?"
gconftool-2 --recursive-unset /apps/panel
rm -rf ~/.gconf/apps/panel
pkill gnome-panel
exit
</code></pre>

<p>}</p>

<p>Main</p>

<h1>END OF Script</h1>

<p>```</p>

<p>create a file called PanelRestore.sh, paste these contents into it and save and then make the file executable and run it.</p>

<p>life saver!</p>
]]></content>
  </entry>
  
</feed>
