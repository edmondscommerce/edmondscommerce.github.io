<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Tag: spotify links | Edmonds Commerce Dev Blog]]></title>
  <link href="http://edmondscommerce.github.io/tag/spotify-links/atom.xml" rel="self"/>
  <link href="http://edmondscommerce.github.io/"/>
  <updated>2014-01-23T17:43:58+00:00</updated>
  <id>http://edmondscommerce.github.io/</id>
  <author>
    <name><![CDATA[EdmondsCommerce Development Team]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Spotify Links Working in Linux]]></title>
    <link href="http://edmondscommerce.github.io/linux/spotify-links-working-in-linux.html"/>
    <updated>2010-06-19T09:41:09+01:00</updated>
    <id>http://edmondscommerce.github.io/linux/spotify-links-working-in-linux</id>
    <content type="html"><![CDATA[<div class="oldpost"><h4>This is an old post. The information it contains is probably out of date or innacurate</h4>
<p>
This is a post that was written a long time ago and is only being kept here for posterity.
You should probably look up more recent blog posts related to the subject you are researching
</p>
</div>


<p><b>Update:</b></p>

<p>if you have installed the native Linux client, just run these three commands in the terminal:</p>

<p>```</p>

<p>gconftool-2 -t string -s /desktop/gnome/url-handlers/spotify/command &ldquo;/usr/bin/spotify -uri %s&rdquo;
gconftool-2 -t bool -s /desktop/gnome/url-handlers/spotify/needs_terminal false
gconftool-2 -t bool -s /desktop/gnome/url-handlers/spotify/enabled true</p>

<p>```</p>

<hr />

<p>To get spotify links (eg playlist links etc) to work in Linux, I found that this solution worked for me:</p>

<p>First you need to create a shell script that will launch spotify for you. Here is the contents of that script:</p>

<p>```</p>

<h1>!/bin/bash</h1>

<p>padsp wine  &ldquo;$HOME/.wine/drive_c/Program Files/Spotify/spotify.exe&rdquo; /uri &ldquo;$@&rdquo;</p>

<p>```</p>

<p>I save the script as spotify.sh, then I set the permissions to executable in Nautilus.</p>

<p>Note the padsp &ndash; if you use Pulse Audio, you need this bit, If you don&rsquo;t then you can remove it. If you try it one way and it doesn&rsquo;t work try it the other way.</p>

<p>Once that&rsquo;s done there are two things you can try.</p>

<p>The first (which seemed to be what worked for me) is to run the following three commands in the command line.</p>

<p>```</p>

<p>gconftool-2 -t string -s /desktop/gnome/url-handlers/spotify/command &ldquo;/home/${USER}/spotify.sh %s&rdquo;
gconftool-2 -s /desktop/gnome/url-handlers/spotify/needs_terminal false -t bool
gconftool-2 -s /desktop/gnome/url-handlers/spotify/enabled true -t bool</p>

<p>```</p>

<p>I also did the following first, though it didn&rsquo;t seem to work on it&rsquo;s own. I&rsquo;m not sure if it is required or not:</p>

<p>In Firefox, go to about:config then you need to add three new config settings (by right clicking in the main area and selecting &lsquo;New&rsquo;).</p>

<p>Type: string
Name: network.protocol-handler.app.spotify
Value: /home/#####/spotify.sh</p>

<p>Type: boolean
Name: network.protocol-handler.external.spotify
Value: true</p>

<p>Type: boolean
Name: network.protocol-handler.expose.spotify
Value: false</p>
]]></content>
  </entry>
  
</feed>
