<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: curl | Edmonds Commerce Dev Blog]]></title>
  <link href="http://edmondscommerce.github.io/category/curl/atom.xml" rel="self"/>
  <link href="http://edmondscommerce.github.io/"/>
  <updated>2015-02-12T15:36:15+00:00</updated>
  <id>http://edmondscommerce.github.io/</id>
  <author>
    <name><![CDATA[EdmondsCommerce Development Team]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[PHP, cURL, CURLOPT FOLLOWLOCATION and open basedir Or Safe Mode]]></title>
    <link href="http://edmondscommerce.github.io/curl/php-curl-curlopt_followlocation-and-open_basedir-or-safe-mode.html"/>
    <updated>2008-07-18T13:09:13+01:00</updated>
    <id>http://edmondscommerce.github.io/curl/php-curl-curlopt_followlocation-and-open_basedir-or-safe-mode</id>
    <content type="html"><![CDATA[<div class="oldpost"><h4>This is post is now quite old and the the information it contains may be out of date or innacurate.</h4>
<p>
If you find any errors or have any suggestions to update the information <a href="http://edmondscommerce.github.io/contact-us/index.html">please let us know</a>
or <a href="https://github.com/edmondscommerce/edmondscommerce.github.io">create a pull request on GitHub</a>
</p>
</div>


<p>If you are trying to get a curl script which needs follow on location functionality to run on a server which has either open_basedir or safe mode enabled you will get an error message similar to the following:</p>

<p><strong>CURLOPT_FOLLOWLOCATION cannot be activated when in safe_mode or an open_basedir is set</strong></p>

<p>After a bit of digging, some kind soul has put a workaround <a href="http://au.php.net/manual/ro/function.curl-setopt.php#71313">here</a></p>

<p>Here is how to use the function</p>

<p>```php</p>

<p>function curl($url){
$go = curl_init($url);
curl_setopt ($go, CURLOPT_URL, $url);
//follow on location problems</p>

<p>if (ini_get(&lsquo;open_basedir&rsquo;) == &lsquo;&rsquo; &amp;&amp; ini_get(&lsquo;safe_mode&rsquo; == &lsquo;Off&rsquo;)){</p>

<p>curl_setopt ($go, CURLOPT_FOLLOWLOCATION, $l);</p>

<p>$syn = curl_exec($go);</p>

<p>}else{</p>

<p>$syn = curl_redir_exec($go);</p>

<p>}</p>

<p>curl_close($go);
return $syn;
}</p>

<p>//follow on location problems workaround</p>

<p>function curl_redir_exec($ch)</p>

<p>{</p>

<p>static $curl_loops = 0;</p>

<p>static $curl_max_loops = 20;</p>

<p>if ($curl_loops++ >= $curl_max_loops)</p>

<p>{</p>

<p>$curl_loops = 0;</p>

<p>return FALSE;</p>

<p>}</p>

<p>curl_setopt($ch, CURLOPT_HEADER, true);</p>

<p>curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);</p>

<p>$data = curl_exec($ch);</p>

<p>list($header, $data) = explode(&ldquo;\n\n&rdquo;, $data, 2);</p>

<p>$http_code = curl_getinfo($ch, CURLINFO_HTTP_CODE);</p>

<p>if ($http_code == 301 || $http_code == 302)</p>

<p>{</p>

<p>$matches = array();</p>

<p>preg_match(&lsquo;/Location:(.*?)\n/&rsquo;, $header, $matches);</p>

<p>$url = @parse_url(trim(array_pop($matches)));</p>

<p>if (!$url)</p>

<p>{</p>

<p>//couldn&rsquo;t process the url to redirect to</p>

<p>$curl_loops = 0;</p>

<p>return $data;</p>

<p>}</p>

<p>$last_url = parse_url(curl_getinfo($ch, CURLINFO_EFFECTIVE_URL));</p>

<p>if (!$url[&lsquo;scheme&rsquo;])</p>

<p>$url[&lsquo;scheme&rsquo;] = $last_url[&lsquo;scheme&rsquo;];</p>

<p>if (!$url[&lsquo;host&rsquo;])</p>

<p>$url[&lsquo;host&rsquo;] = $last_url[&lsquo;host&rsquo;];</p>

<p>if (!$url[&lsquo;path&rsquo;])</p>

<p>$url[&lsquo;path&rsquo;] = $last_url[&lsquo;path&rsquo;];</p>

<p>$new_url = $url[&lsquo;scheme&rsquo;] . &lsquo;://&rsquo; . $url[&lsquo;host&rsquo;] . $url[&lsquo;path&rsquo;] . ($url[&lsquo;query&rsquo;]?&lsquo;?&rsquo;.$url[&lsquo;query&rsquo;]:&lsquo;&rsquo;);</p>

<p>curl_setopt($ch, CURLOPT_URL, $new_url);</p>

<p>return curl_redir_exec($ch);</p>

<p>} else {</p>

<p>$curl_loops=0;</p>

<p>return $data;</p>

<p>}</p>

<p>}</p>

<p>```</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[PHP Save Images Using cURL]]></title>
    <link href="http://edmondscommerce.github.io/php/curl/php-save-images-using-curl.html"/>
    <updated>2008-03-18T14:53:47+00:00</updated>
    <id>http://edmondscommerce.github.io/php/curl/php-save-images-using-curl</id>
    <content type="html"><![CDATA[<div class="oldpost"><h4>This is post is now quite old and the the information it contains may be out of date or innacurate.</h4>
<p>
If you find any errors or have any suggestions to update the information <a href="http://edmondscommerce.github.io/contact-us/index.html">please let us know</a>
or <a href="https://github.com/edmondscommerce/edmondscommerce.github.io">create a pull request on GitHub</a>
</p>
</div>


<p>Some hosts disabled the ini setting allow_url_fopen. This also means that the ability to easily grab images by calling imagecreatefromjpeg($img) where $img is a url for an external image does not work.</p>

<p>This is a shame as this technique is pretty easy..</p>

<p>```php</p>

<p>$remote_img = &lsquo;<a href="http://www.somwhere.com/images/image.jpg">http://www.somwhere.com/images/image.jpg</a>&rsquo;;
$img = imagecreatefromjpeg($remote_img);
$path = &lsquo;images/&rsquo;;
imagejpeg($img, $path);</p>

<p>```</p>

<p>However I was tearing my hair out trying to get a product feed integration system to work on a host that has disabled the allow_url_fopen mechanism which meant the above wouldnt work.</p>

<p>Thankfully cURL was still working. So here is the function I made for grabbing and saving an image using cURL instead:</p>

<p>```php</p>

<p>//Alternative Image Saving Using cURL seeing as allow_url_fopen is disabled &ndash; bummer
function save_image($img,$fullpath){</p>

<pre><code>$ch = curl_init ($img);
curl_setopt($ch, CURLOPT_HEADER, 0);
curl_setopt($ch, CURLOPT_RETURNTRANSFER, 1);
curl_setopt($ch, CURLOPT_BINARYTRANSFER,1);
$rawdata=curl_exec($ch);
curl_close ($ch);
if(file_exists($fullpath)){
    unlink($fullpath);
}
$fp = fopen($fullpath,'x');
fwrite($fp, $rawdata);
fclose($fp); 
</code></pre>

<p>}</p>

<p>```</p>

<p>Another Problem Solved :&ndash;)</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Using cURL with XAMPP]]></title>
    <link href="http://edmondscommerce.github.io/curl/xampp/using-curl-with-xampp.html"/>
    <updated>2008-02-27T11:36:51+00:00</updated>
    <id>http://edmondscommerce.github.io/curl/xampp/using-curl-with-xampp</id>
    <content type="html"><![CDATA[<div class="oldpost"><h4>This is post is now quite old and the the information it contains may be out of date or innacurate.</h4>
<p>
If you find any errors or have any suggestions to update the information <a href="http://edmondscommerce.github.io/contact-us/index.html">please let us know</a>
or <a href="https://github.com/edmondscommerce/edmondscommerce.github.io">create a pull request on GitHub</a>
</p>
</div>


<p>If you have read through this blog, you will realise that I am a big fan of the cURL library for PHP. Also, when using windows to code &ndash; I like to use the XAMPP package to give me easy access to an Apache, PHP and MySQL stack.</p>

<p>So here is my quick guide to getting XAMPP to work with cURL. Its really easy:</p>

<ol>
<li><p>Navigate to your root XAMPP folder</p></li>
<li><p>Search for all files and folder with this search query: php*.ini</p></li>
<li><p>Open up all of the files in your favourite windows text editor (try notepad++)</p></li>
<li><p>Do a find and replace:  Find <em>;extension=php_curl.dll </em>and replace with<em> extension=php_curl.dll </em>(you are just removing the semi colon). Make sure you select to replace in all opened documents</p></li>
<li><p>File &ndash;&gt; Save All Documents</p></li>
<li><p>Restart Apache using the XAMPP control panel</p></li>
</ol>


<p>Thats it &ndash; enjoy using cURL.</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Building Spiders: Grab Data, Post Forms and Interact with Web Sites Automatically]]></title>
    <link href="http://edmondscommerce.github.io/php/curl/spidering/firefox/programming/building-spiders-grab-data-post-forms-and-interact-with-web-sites-automatically.html"/>
    <updated>2008-02-14T11:56:16+00:00</updated>
    <id>http://edmondscommerce.github.io/php/curl/spidering/firefox/programming/building-spiders-grab-data-post-forms-and-interact-with-web-sites-automatically</id>
    <content type="html"><![CDATA[<div class="oldpost"><h4>This is post is now quite old and the the information it contains may be out of date or innacurate.</h4>
<p>
If you find any errors or have any suggestions to update the information <a href="http://edmondscommerce.github.io/contact-us/index.html">please let us know</a>
or <a href="https://github.com/edmondscommerce/edmondscommerce.github.io">create a pull request on GitHub</a>
</p>
</div>


<p>One of the most useful and powerful things you can do with PHP is to create a programme which will simulate a web browser and can grab data, post data to forms and generally interact with other web sites &ndash; automatically.</p>

<p>For PHP to be able to work like this it must have the CURL library installed and active. It is the CURL library which actually handles all of the interaction and PHP is my scripting language of choice for interacting with CURL.</p>

<p>A simple CURL function is like this:</p>

<p>```php</p>

<p>function curl($url){</p>

<p>$timeout = &lsquo;300&rsquo;; //how long before CURL gives up on this page
$go = curl_init();
curl_setopt ($go, CURLOPT_URL, $url);
curl_setopt ($go, CURLOPT_RETURNTRANSFER, 1);
curl_setopt ($go, CURLOPT_FOLLOWLOCATION, 1);
curl_setopt ($go, CURLOPT_TIMEOUT, $timeout);
$page = curl_exec($go);
curl_close($go);
return $page;</p>

<p>}</p>

<p>```</p>

<p>This function when called and echoed will output the entire html of the $url specified.</p>

<p>For grabbing data from this page to be inserted into a database (for example when spidering a suppliers web site for product information to be inserted into your site) we then use regular expressions to find what we are looking for and then insert that into the database.</p>

<p>so for example if we wanted to grab the product title and we knew that this was wrapped in a h1 tag with the class &ldquo;product title&rdquo; we could use this regexp to grab this:</p>

<p>```php</p>

<p>$page = curl($url);</p>

<p>$pattern = &lsquo;%</p>

<h1 class="product_title">(.+?)</h1>


<p>%i';</p>

<p>preg_match($pattern,$page,$matches);</p>

<p>print_r($matches); //we can see the entire array of matches and choose which we want to insert into the database</p>

<p>```</p>

<p>We can also Post data to web sites using curl. This allows us to do all kinds of things including grabbing data that is displayed on the submission of post forms. Here is an example Curl Post Function:
```php</p>

<p>function curl_post($url,$post_data){</p>

<p>$timeout = &lsquo;300&rsquo;; //how long before CURL gives up on this page
$go = curl_init();
curl_setopt ($go, CURLOPT_URL, $url);
curl_setopt ($go, CURLOPT_RETURNTRANSFER, 1);
curl_setopt ($go, CURLOPT_FOLLOWLOCATION, 1);
curl_setopt ($go, CURLOPT_TIMEOUT, $timeout);
//now for the post section
curl_setopt($go, CURLOPT_POST, true);</p>

<p>curl_setopt($go, CURLOPT_POSTFIELDS, $post_data);
$page = curl_exec($go);
curl_close($go);
return $page;
}</p>

<p>```</p>

<p>It can be tricky to figure out exactly what data should be in the post string. To help you out though is this incredibly handy addon for firefox: <a href="https://addons.mozilla.org/en-US/firefox/addon/3829" target="_blank">Live Http Headers</a>.</p>

<p>This addon lets you see exactly what is going on between your browser and the web site you are visiting. This can quickly and easily give you the information you need to replicate the same behaviour with your CURL script.</p>

<p>Edmonds Commerce specialise in working with PHP and CURL. If you have any spidering, screen scraping or other application that requires PHP to actively interact with other web sites &ndash; <a href="http://www.edmondscommerce.co.uk/contact-about-curl-and-php.html">get in touch today</a> to see how we can help you benefit from this incredibly powerful technique.</p>

<p>Related Resources</p>

<p><a href="http://www.phpfour.com/blog/2008/01/20/php-http-class/" rel="nofollow"><a href="http://www.phpfour.com/blog/2008/01/20/php-http-class/">http://www.phpfour.com/blog/2008/01/20/php-http-class/</a></a></p>

<p><a href="http://www.phpclasses.org/browse/package/1988.html" rel="nofollow"><a href="http://www.phpclasses.org/browse/package/1988.html">http://www.phpclasses.org/browse/package/1988.html</a></a></p>

<p><a href="http://www.phpit.net/article/using-curl-php/" rel="nofollow"><a href="http://www.phpit.net/article/using-curl-php/">http://www.phpit.net/article/using-curl-php/</a></a></p>

<p><a href="http://skeymedia.com/intro-to-curl-with-php/" rel="nofollow"><a href="http://skeymedia.com/intro-to-curl-with-php/">http://skeymedia.com/intro-to-curl-with-php/</a></a></p>
]]></content>
  </entry>
  
</feed>
